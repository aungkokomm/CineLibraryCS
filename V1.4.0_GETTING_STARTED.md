# v1.4.0 Development - Getting Started Guide

## 📍 Where You Are Now

✅ **v1.3.0 Released** to GitHub  
✅ **Branch Created**: `feature/v1.4-ui-enhancements`  
✅ **Backend Ready**: All database methods, filters, statistics  
🚀 **Next**: Build UI for v1.4.0 features

---

## 🎯 Your First Task: Build Statistics Dashboard

The Statistics Dashboard is the **highest priority** and has the most **user-facing impact**.

### What You're Building
A dashboard showing:
- 📊 Movies organized by decade
- 👥 Top 10 directors by movie count
- 👥 Top 10 actors by appearance count
- ⏱️ Total hours of movies in library
- 📈 What % of your collection you've watched
- 📅 Average movies per year

**All backend methods are already built and tested!**

---

## 🔧 Step-by-Step: Build Statistics Dashboard

### Step 1: Create the XAML Layout (30-45 mins)

Create a new file: `Views/StatisticsPage.xaml`

```xaml
<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CineLibraryCS.Views.StatisticsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer>
        <StackPanel Padding="20" Spacing="20">
            <!-- Title -->
            <TextBlock Text="📊 Library Statistics" 
                       FontSize="28" FontWeight="SemiBold"
                       Margin="0,0,0,10"/>

            <!-- Refresh Button -->
            <Button x:Name="RefreshButton" Content="🔄 Refresh Statistics" 
                    Click="OnRefreshClick" 
                    HorizontalAlignment="Left"/>

            <!-- Total Movies Card -->
            <Border CornerRadius="8" 
                    Background="{ThemeResource CardBackgroundFillColorDefault}"
                    BorderBrush="{ThemeResource CardBorderBrush}"
                    BorderThickness="1"
                    Padding="20">
                <StackPanel>
                    <TextBlock Text="📚 Total Movies in Library" 
                               FontSize="16" FontWeight="SemiBold"/>
                    <TextBlock x:Name="TotalMoviesText" Text="Loading..." 
                               FontSize="32" FontWeight="Bold"
                               Foreground="{ThemeResource AccentTextFillColorPrimary}"
                               Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

            <!-- Total Runtime Card -->
            <Border CornerRadius="8" 
                    Background="{ThemeResource CardBackgroundFillColorDefault}"
                    BorderBrush="{ThemeResource CardBorderBrush}"
                    BorderThickness="1"
                    Padding="20">
                <StackPanel>
                    <TextBlock Text="⏱️ Total Runtime" 
                               FontSize="16" FontWeight="SemiBold"/>
                    <StackPanel Orientation="Horizontal" Spacing="20" Margin="0,10,0,0">
                        <StackPanel>
                            <TextBlock Text="Hours:" FontSize="12"/>
                            <TextBlock x:Name="RuntimeHoursText" Text="Loading..." 
                                       FontSize="28" FontWeight="Bold"
                                       Foreground="{ThemeResource AccentTextFillColorPrimary}"/>
                        </StackPanel>
                        <StackPanel>
                            <TextBlock Text="Days:" FontSize="12"/>
                            <TextBlock x:Name="RuntimeDaysText" Text="Loading..." 
                                       FontSize="28" FontWeight="Bold"
                                       Foreground="{ThemeResource AccentTextFillColorPrimary}"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Watch Progress Card -->
            <Border CornerRadius="8" 
                    Background="{ThemeResource CardBackgroundFillColorDefault}"
                    BorderBrush="{ThemeResource CardBorderBrush}"
                    BorderThickness="1"
                    Padding="20">
                <StackPanel>
                    <TextBlock Text="👁️ Watch Progress" 
                               FontSize="16" FontWeight="SemiBold"/>
                    <TextBlock x:Name="WatchProgressText" Text="Loading..." 
                               FontSize="24" FontWeight="Bold"
                               Foreground="{ThemeResource AccentTextFillColorPrimary}"
                               Margin="0,10,0,0"/>
                    <ProgressBar x:Name="WatchProgressBar" 
                                 Margin="0,10,0,0" Height="8" CornerRadius="4"/>
                </StackPanel>
            </Border>

            <!-- Movies by Decade -->
            <Border CornerRadius="8" 
                    Background="{ThemeResource CardBackgroundFillColorDefault}"
                    BorderBrush="{ThemeResource CardBorderBrush}"
                    BorderThickness="1"
                    Padding="20">
                <StackPanel>
                    <TextBlock Text="📅 Movies by Decade" 
                               FontSize="16" FontWeight="SemiBold"
                               Margin="0,0,0,10"/>
                    <ListView x:Name="DecadeListView" 
                              ItemsSource="{x:Bind DecadeStats}"
                              SelectionMode="None"
                              IsItemClickEnabled="False">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="local:DecadeStatItem">
                                <Grid ColumnSpacing="10" Margin="0,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="80"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="50"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" 
                                               Text="{x:Bind Decade}" 
                                               FontWeight="SemiBold"/>
                                    <ProgressBar Grid.Column="1" 
                                                 Value="{x:Bind Percentage}"
                                                 Maximum="100" Height="6" CornerRadius="3"/>
                                    <TextBlock Grid.Column="2" 
                                               Text="{x:Bind Count}" 
                                               TextAlignment="Right"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </Border>

            <!-- Top Directors -->
            <Border CornerRadius="8" 
                    Background="{ThemeResource CardBackgroundFillColorDefault}"
                    BorderBrush="{ThemeResource CardBorderBrush}"
                    BorderThickness="1"
                    Padding="20">
                <StackPanel>
                    <TextBlock Text="🎬 Top Directors" 
                               FontSize="16" FontWeight="SemiBold"
                               Margin="0,0,0,10"/>
                    <ListView x:Name="DirectorsListView" 
                              ItemsSource="{x:Bind TopDirectors}"
                              SelectionMode="None"
                              IsItemClickEnabled="False"
                              MaxHeight="300">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="local:GenreFacet">
                                <Grid ColumnSpacing="10" Margin="0,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="50"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" 
                                               Text="{x:Bind Name}"/>
                                    <TextBlock Grid.Column="1" 
                                               Text="{x:Bind Count}" 
                                               TextAlignment="Right"
                                               Foreground="{ThemeResource TextFillColorSecondary}"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </Border>

            <!-- Top Actors -->
            <Border CornerRadius="8" 
                    Background="{ThemeResource CardBackgroundFillColorDefault}"
                    BorderBrush="{ThemeResource CardBorderBrush}"
                    BorderThickness="1"
                    Padding="20">
                <StackPanel>
                    <TextBlock Text="👥 Top Actors" 
                               FontSize="16" FontWeight="SemiBold"
                               Margin="0,0,0,10"/>
                    <ListView x:Name="ActorsListView" 
                              ItemsSource="{x:Bind TopActors}"
                              SelectionMode="None"
                              IsItemClickEnabled="False"
                              MaxHeight="300">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="local:GenreFacet">
                                <Grid ColumnSpacing="10" Margin="0,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="50"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" 
                                               Text="{x:Bind Name}"/>
                                    <TextBlock Grid.Column="1" 
                                               Text="{x:Bind Count}" 
                                               TextAlignment="Right"
                                               Foreground="{ThemeResource TextFillColorSecondary}"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
```

### Step 2: Create Data Models (10 mins)

Add this to `Models/Collection.cs` or create `Models/StatisticsModels.cs`:

```csharp
namespace CineLibraryCS.Models
{
    public class DecadeStatItem
    {
        public int Decade { get; set; }
        public int Count { get; set; }
        public double Percentage { get; set; }
    }
}
```

### Step 3: Create the CodeBehind (45 mins)

Create `Views/StatisticsPage.xaml.cs`:

```csharp
using CineLibraryCS.Models;
using CineLibraryCS.Services;
using Microsoft.UI.Xaml;
using Microsoft.UI.Xaml.Controls;
using System.Collections.ObjectModel;

namespace CineLibraryCS.Views
{
    public sealed partial class StatisticsPage : Page
    {
        private readonly DatabaseService _db;
        private int _totalMovies = 0;

        public ObservableCollection<DecadeStatItem> DecadeStats { get; set; } = new();
        public ObservableCollection<GenreFacet> TopDirectors { get; set; } = new();
        public ObservableCollection<GenreFacet> TopActors { get; set; } = new();

        public StatisticsPage()
        {
            this.InitializeComponent();
            _db = AppState.Services.GetRequiredService<DatabaseService>();
            this.Loaded += OnPageLoaded;
        }

        private async void OnPageLoaded(object sender, RoutedEventArgs e)
        {
            await LoadStatisticsAsync();
        }

        private async Task LoadStatisticsAsync()
        {
            try
            {
                // Get total movies
                _totalMovies = _db.GetAllMovies().Count;
                TotalMoviesText.Text = _totalMovies.ToString();

                // Get runtime
                int totalHours = _db.GetTotalRuntimeHours();
                RuntimeHoursText.Text = totalHours.ToString();
                RuntimeDaysText.Text = (totalHours / 24.0).ToString("F1");

                // Get watch progress
                var (watched, total, percentage) = _db.GetWatchProgress();
                WatchProgressText.Text = $"{watched} / {total} watched ({percentage:F1}%)";
                WatchProgressBar.Value = percentage;

                // Get decade stats
                DecadeStats.Clear();
                var decadeData = _db.GetMoviesByDecade();
                int maxCount = decadeData.Max(d => d.Count);
                foreach (var (decade, count) in decadeData.OrderByDescending(d => d.Decade))
                {
                    DecadeStats.Add(new DecadeStatItem
                    {
                        Decade = decade,
                        Count = count,
                        Percentage = (double)count / maxCount * 100
                    });
                }

                // Get top directors
                TopDirectors.Clear();
                foreach (var director in _db.GetTopDirectors(10))
                {
                    TopDirectors.Add(director);
                }

                // Get top actors
                TopActors.Clear();
                foreach (var actor in _db.GetTopActors(10))
                {
                    TopActors.Add(actor);
                }
            }
            catch (Exception ex)
            {
                System.Diagnostics.Debug.WriteLine($"Error loading statistics: {ex.Message}");
            }
        }

        private void OnRefreshClick(object sender, RoutedEventArgs e)
        {
            _ = LoadStatisticsAsync();
        }
    }
}
```

### Step 4: Add Navigation (15 mins)

**Update `MainWindow.xaml`** - Add sidebar button:

Find the sidebar navigation buttons and add:

```xaml
<!-- Statistics Button (add after Watchlist button) -->
<Button x:Name="BtnStatistics" 
        Style="{StaticResource NavItemStyle}" 
        Click="OnNavStatistics">
    <StackPanel Orientation="Horizontal" Spacing="8">
        <TextBlock Text="📊" VerticalAlignment="Center"/>
        <TextBlock Text="Statistics" VerticalAlignment="Center"/>
    </StackPanel>
</Button>
```

**Update `MainWindow.xaml.cs`** - Add click handler:

```csharp
private void OnNavStatistics(object sender, RoutedEventArgs e)
{
    ContentFrame.Navigate(typeof(Views.StatisticsPage));
    SidebarTitle.Text = "📊 Statistics";
}
```

### Step 5: Test It! (15 mins)

1. **Build the solution** - Should compile with 0 errors
2. **Run the app** - Press F5
3. **Click "Statistics"** in the sidebar
4. **Verify**:
   - [ ] Dashboard loads without errors
   - [ ] All numbers display correctly
   - [ ] Decade list shows data
   - [ ] Directors list shows data
   - [ ] Actors list shows data
   - [ ] Refresh button works
   - [ ] UI looks good

---

## 🎨 UI Polish Tips

- Use consistent spacing (8px, 16px, 20px grid)
- Keep card backgrounds subtle (use theme brushes)
- Add icons next to titles for visual interest
- Use ProgressBars for visual data representation
- Consider animations on refresh
- Test on both light and dark themes

---

## ✅ Checklist for This Feature

- [ ] StatisticsPage.xaml created
- [ ] StatisticsPage.xaml.cs created
- [ ] DecadeStatItem model created
- [ ] Navigation button added to MainWindow
- [ ] Click handler added to MainWindow.xaml.cs
- [ ] Build succeeds (0 errors, 0 warnings)
- [ ] Statistics page loads
- [ ] Data displays correctly
- [ ] Refresh button works
- [ ] UI looks professional

---

## 🚀 What's Next After This?

Once Statistics Dashboard is complete:

1. **Commit your work**:
```powershell
git add .
git commit -m "feat: Add Statistics Dashboard for v1.4.0"
```

2. **Move to Phase 2**: Actor/Director Filter UI
   - See `V1.4.0_TASK_LIST.md` Task 2.1

3. **Or skip ahead** if you want to tackle a different feature

---

## 💡 Debugging Tips

**If page doesn't load:**
- Check `InitializeComponent()` is called in constructor
- Verify namespace in xaml is correct
- Check Build output for XAML parsing errors

**If data doesn't show:**
- Add `System.Diagnostics.Debug.WriteLine()` in LoadStatisticsAsync()
- Check if `_db` is getting DatabaseService correctly
- Verify database has movie data

**If numbers seem wrong:**
- Call GetAllMovies() and verify count manually
- Check GetMoviesByDecade() returns data
- Test queries in database directly

---

## 📚 Reference Files

- `Services/DatabaseService.cs` - All the query methods (GetTotalRuntimeHours, GetTopDirectors, etc.)
- `Views/LibraryPage.xaml` - Good UI layout example
- `Views/MovieDetailDialog.xaml` - Good card/border styling example
- `Models/Collection.cs` - GenreFacet structure

**Good luck! You've got this! 🚀**
