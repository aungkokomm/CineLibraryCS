# CineLibrary v1.4.0 Development Roadmap

## 🎯 Project Status
- **Current Version**: v1.3.0 (released on GitHub)
- **Next Version**: v1.4.0 (UI enhancements)
- **Branch**: `feature/v1.4-ui-enhancements`
- **ETA**: 10-20 hours of development

## 📊 Features Already Ready (v1.3.0 Backend)
✅ **Statistics Methods** - 6 database queries for analytics
✅ **Actor/Director Filters** - Backend support implemented
✅ **Database Indexes** - 8 SQL indexes for 5-10x performance
✅ **Watchlist Feature** - Fully functional with sidebar button
✅ **Automatic Migrations** - Schema updates on first run

## 🎨 UI Enhancements to Implement (v1.4.0)

### Priority 1: Statistics Dashboard (⭐ HIGHEST IMPACT)
**Status**: Backend ready, UI needs implementation

**What to build:**
- New page: `Views/StatisticsPage.xaml` and `Views/StatisticsPage.xaml.cs`
- Dashboard showing:
  - 📊 Movies by decade (bar chart or timeline)
  - 👥 Top 10 directors (with movie counts)
  - 👥 Top 10 actors (with appearance counts)
  - ⏱️ Total runtime hours
  - 📈 Watch progress percentage
  - 📅 Average movies per year

**Backend Methods Available:**
```csharp
// All implemented in DatabaseService.cs
GetMoviesByDecade()      // Returns: (decade, count)
GetTopDirectors(limit)   // Returns: List<GenreFacet>
GetTopActors(limit)      // Returns: List<GenreFacet>
GetTotalRuntimeHours()   // Returns: int
GetWatchProgress()       // Returns: (watched, total) percentage
```

**UI Components Needed:**
- StackPanel with cards for each metric
- Simple charts (bars/graphs) for visualization
- Refresh button to reload statistics
- Navigation button in sidebar

**Estimated Time**: 3-4 hours

---

### Priority 2: Actor/Director Filter UI (⭐ HIGH IMPACT)
**Status**: Backend ready, UI needs implementation

**What to build:**
- Clickable director/actor names in `MovieDetailDialog`
- When clicked: Filter library by that person
- Show applied filter badge in LibraryPage
- "Clear Actor/Director Filter" button

**Backend Methods Available:**
```csharp
// All implemented in LibraryViewModel
FilterByActor(name)       // Filter by actor name
FilterByDirector(name)    // Filter by director name
// GetMovies() already supports ActorFilter and DirectorFilter in ListOptions
```

**UI Components Needed:**
- Update `MovieDetailDialog.xaml.cs` to detect clicks on director/actor names
- Add hyperlink-style TextBlock (underlined, colored text) for actors/directors
- Show filter status in LibraryPage (e.g., "Showing movies with: Tom Hanks")
- Clear button to reset filter

**Estimated Time**: 2-3 hours

---

### Priority 3: Keyboard Shortcuts Dialog (⭐ MEDIUM IMPACT)
**Status**: Concept ready, UI needs implementation

**What to build:**
- New dialog: `Views/KeyboardShortcutsDialog.xaml`
- Display all keyboard shortcuts in organized table
- Add "Keyboard Shortcuts" button to titlebar

**Shortcuts to Document:**
| Shortcut | Action |
|----------|--------|
| Ctrl+F | Focus search box |
| Ctrl+L | Show/hide sidebar |
| Ctrl+B | Toggle theme (light/dark) |
| Ctrl+Q | Quit application |
| Delete | Delete selected movie |
| F5 | Refresh library |
| Enter | Open selected movie details |
| Escape | Close dialogs |

**UI Components Needed:**
- DataGrid or ListView showing shortcut table
- Dialog window with close button
- Menu item or toolbar button to open

**Estimated Time**: 1-2 hours

---

### Priority 4: Bulk Operations (⭐ NICE TO HAVE)
**Status**: Concept ready, full implementation needed

**What to build:**
- Multi-select checkbox in LibraryPage
- Bulk actions: Add to Watchlist, Mark as Watched, Delete, Set Genre
- Status bar showing "3 movies selected"

**Backend Methods Needed:**
```csharp
// New methods to add to DatabaseService
SetMultipleMoviesWatchlist(movieIds, isWatchlist)
SetMultipleMoviesWatched(movieIds, watched)
DeleteMultipleMovies(movieIds)
SetMultipleMoviesGenre(movieIds, genre)
```

**UI Components Needed:**
- Checkbox column in MovieRowControl
- Selection state management in LibraryViewModel
- Bulk action buttons (Add to Watchlist, Mark Watched, etc.)
- Status indicator

**Estimated Time**: 4-5 hours

---

## 📋 Implementation Plan

### Phase 1: Statistics Dashboard (Highest Priority - 3-4 hours)
```
1. Create Views/StatisticsPage.xaml with dashboard layout
2. Create Views/StatisticsPage.xaml.cs with data binding
3. Add navigation in MainWindow (sidebar button or menu)
4. Test all statistics queries work correctly
5. Polish UI with Fluent Design (Mica backdrop, proper spacing)
```

### Phase 2: Actor/Director Filter UI (2-3 hours)
```
1. Update MovieDetailDialog to show clickable names
2. Add click handlers to navigate back to library with filter
3. Display active filter status in LibraryPage
4. Add clear filter button
5. Test filtering by multiple people
```

### Phase 3: Keyboard Shortcuts Dialog (1-2 hours)
```
1. Create Views/KeyboardShortcutsDialog.xaml
2. Add Help menu or button to show dialog
3. List all application shortcuts
4. Test keyboard navigation within dialog
```

### Phase 4: Bulk Operations (4-5 hours) - Optional
```
1. Add checkbox column to MovieRowControl
2. Create bulk selection state management
3. Implement database methods for bulk updates
4. Add toolbar with bulk action buttons
5. Test multi-select and bulk operations
```

---

## 🛠️ Technical Details

### File Changes Summary

**New Files to Create:**
- `Views/StatisticsPage.xaml` (250+ lines)
- `Views/StatisticsPage.xaml.cs` (150+ lines)
- `Views/KeyboardShortcutsDialog.xaml` (100+ lines)
- `Views/KeyboardShortcutsDialog.xaml.cs` (50+ lines)

**Files to Modify:**
- `Services/DatabaseService.cs` (add bulk operation methods)
- `ViewModels/LibraryViewModel.cs` (add selection state management)
- `MainWindow.xaml` (add statistics/help buttons)
- `MainWindow.xaml.cs` (add navigation handlers)
- `Views/MovieDetailDialog.xaml.cs` (make names clickable)
- `Views/MovieRowControl.xaml` (add checkbox column - if bulk ops)

**Version Updates:**
- `CineLibraryCS.csproj` → v1.4.0
- `installer/CineLibrary.iss` → v1.4.0

---

## ✅ Quality Checklist Before Release

- [ ] All Statistics queries return correct data
- [ ] Statistics dashboard displays properly on various resolutions
- [ ] Actor/Director names are clickable and filter works
- [ ] Keyboard shortcuts dialog lists all shortcuts
- [ ] All keyboard shortcuts actually work
- [ ] Build completes with 0 errors, 0 warnings
- [ ] No breaking changes to v1.3.0 features
- [ ] Backward compatible with existing databases
- [ ] Code reviewed for performance
- [ ] UI tested on Windows 10 & 11
- [ ] InnoSetup installer builds successfully
- [ ] Version updated everywhere (csproj, iss, MainWindow)

---

## 📚 Key Code References

### Statistics Methods (Already Implemented in DatabaseService.cs)
```csharp
public List<(int Decade, int Count)> GetMoviesByDecade()
public List<GenreFacet> GetTopDirectors(int limit = 10)
public List<GenreFacet> GetTopActors(int limit = 10)
public int GetTotalRuntimeHours()
public (int Watched, int Total, double Percentage) GetWatchProgress()
```

### Filter Methods (Already in LibraryViewModel)
```csharp
public void FilterByActor(string name)
public void FilterByDirector(string name)
public void ShowWatchlist()
```

### Database Query Support
```csharp
public class ListOptions
{
    public string? Genre { get; set; }
    public string? DriveSerial { get; set; }
    public int? CollectionId { get; set; }
    public bool FavoritesOnly { get; set; }
    public bool IsWatchlistOnly { get; set; }
    public string? Actor { get; set; }           // ← NEW (v1.3.0)
    public string? Director { get; set; }        // ← NEW (v1.3.0)
}
```

---

## 🚀 Next Steps

1. **Confirm priorities** - Review this roadmap and approve
2. **Start Phase 1** - Begin Statistics Dashboard implementation
3. **Iterative testing** - Test each feature as it's built
4. **Community feedback** - Consider user input during development
5. **Beta testing** - Allow select users to test v1.4.0 before release
6. **v1.4.0 Release** - Tag and publish when ready

---

## 📈 Long-term Roadmap (v1.5.0+)

Beyond v1.4.0, consider:
- **Advanced search** - Full-text search with filters
- **Export features** - CSV/JSON export of library
- **Import/merge** - Merge multiple CineLibrary databases
- **Cloud sync** - Optional sync across devices (OneDrive, etc.)
- **IMDb integration** - Auto-fetch movie details from IMDb
- **Duplicate detection** - Identify movies added multiple times
- **Recommendations** - ML-based movie recommendations
- **API** - REST API for third-party integrations
- **Plugin system** - Allow community extensions
- **Dark mode refinements** - Additional theme options

---

## 💡 Development Notes

- All backend features are ready and tested
- UI work can proceed independently
- Backward compatibility maintained (no breaking changes)
- Database schema is stable (migrations handle updates)
- Target .NET 8 throughout (no version changes needed)
- WinUI 3 provides all UI components needed

**Questions?** Review the ANALYSIS_AND_RECOMMENDATIONS.md or IMPLEMENTATION_CODE_EXAMPLES.md for more details.
